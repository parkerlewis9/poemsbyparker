include ./includes/head.pug
body.container
    //- Pull top fives to the right
    //- Add About type section
    h1 Welcome to poemsbyparker
    div
        p 5 Most Recent Collections
        ul
            each collection in collections
                li 
                    a(href=`/collections?name=${collection.name}`) #{collection.name}

    div
        p 5 Most Recent Poems
            ul
                each poem in poems
                    li 
                        a(href=`/collections/poems/?collection=${poem.collection_name}&title=${poem.title}`) #{poem.title}

    div
        a(href="/collections") All Collections

    div
        a(href="/poems") All Poems

